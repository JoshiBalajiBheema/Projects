with open("Name of your TEXT file here along with it's file extension") as file:
    count = 0
    text = file.read()
    for i in text:
        if i.isupper():
            count += 1
    print(count)
